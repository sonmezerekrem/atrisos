name: "{{.Name}}"
description: ""
tags: []
meta: {}
{{- if .domain}}

domains:
  - service: "web"
    host: "{{.domain}}"
    port: {{.port}}
    tls: {{.tls}}
{{- end}}

update:
  mode: manual

auto_start: false

backup:
  enabled: false
